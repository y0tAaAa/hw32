# 📑 Індекс проекту MovieFinder

## 👋 Ласкаво просимо до проекту HW32!

## 📂 Структура проекту

```
hw32/
│
├── 🎯 ГОЛОВНІ ФАЙЛИ
│   ├── index.html         ← Відкрийте це в браузері!
│   ├── styles.css         Весь дизайн (614 рядків)
│   └── script.js          Вся логіка (375 рядків)
│
├── 📖 ДОКУМЕНТАЦІЯ
│   ├── INDEX.md           Цей файл - швидкий орієнтир
│   ├── QUICK_START.md     Швидкий старт за 5 хвилин
│   ├── README.md          Повна документація
│   ├── REQUIREMENTS.md    Матриця вимог завдання
│   ├── FEATURES.md        Реалізовані особливості
│   └── SETUP.txt          Інструкція для викладача
```

---

## 📖 Що де знайти

### Для запуску додатку
| Що потрібно | Де знайти | Часу |
|------------|----------|------|
| Швидкий запуск | `QUICK_START.md` | 5 хв |
| Налаштування API | `QUICK_START.md` шаг 2 | 1 хв |
| Інструкція для викладача | `SETUP.txt` | 3 хв |

### Для розуміння коду
| Що потрібно | Де знайти | Файл |
|------------|----------|------|
| Загальна структура | `README.md` | 📖 |
| Особливості | `FEATURES.md` | 🎯 |
| HTML розмітка | `index.html` | 📄 |
| CSS стилі | `styles.css` | 🎨 |
| JavaScript логіка | `script.js` | ⚙️ |

### Для перевірки вимог
| Вимога | Де знайти |
|--------|-----------|
| 1️⃣ API інтеграція | `REQUIREMENTS.md` + `script.js` (84-115) |
| 2️⃣ LiveSearch | `REQUIREMENTS.md` + `script.js` (64-79) |
| 3️⃣ Результати | `REQUIREMENTS.md` + `script.js` (137-155) |
| 4️⃣ UI/UX | `REQUIREMENTS.md` + `styles.css` |
| 5️⃣ Адаптив | `REQUIREMENTS.md` + `styles.css` (514-582) |
| 6️⃣ Error handling | `REQUIREMENTS.md` + `script.js` (301-330) |

---

## 🎯 Основні файли для опанування

### 1. `index.html` - Struktura сторінки
- 🔍 90 рядків HTML
- 📱 Семантична розмітка
- 🎨 Всі елементи інтерфейсу

**Основні компоненти:**
```html
<!-- Пошукове поле -->
<input id="searchInput" placeholder="Введіть назву фільму...">

<!-- Контейнер результатів -->
<div id="resultsContainer" class="results-grid"></div>

<!-- Модальне вікно -->
<div id="movieModal" class="modal"></div>
```

### 2. `styles.css` - Весь дизайн
- 🎨 614 рядків CSS
- 📱 3 media queries для адаптиву
- ✨ 5+ анімацій

**Основні розділи:**
```
:root              - CSS змінні для теми
.container         - Основний контейнер
.header            - Заголовок
.search-section    - Пошуковий рядок
.results-grid      - Сітка результатів
.movie-card        - Карточка фільму
.modal             - Модальне вікно
@media             - Адаптивний дизайн
@keyframes         - Анімації
```

### 3. `script.js` - Вся логіка
- ⚙️ 375 рядків JavaScript
- 🔗 Інтеграція з OMDb API
- 🔍 LiveSearch функціонал
- ⚠️ Обробка помилок

**Основні функції:**
```javascript
CONFIG              - Конфігурація
init()              - Ініціалізація
handleSearchInput()  - LiveSearch логіка
performSearch()     - Пошук фільмів
fetchMovies()       - API запит 1
fetchMovieDetails() - API запит 2
displayResults()    - Показ результатів
showMovieDetails()  - Модаль з деталями
handleError()       - Обробка помилок
```

---

## 🔧 Налаштування в 3 кроки

### Крок 1: Отримайте API key
1. Перейдіть на https://www.omdbapi.com/
2. Отримайте безплатний key

### Крок 2: Встановіть в коді
1. Відкрийте `script.js`
2. Рядок 7: `API_KEY: 'ВАШ_KEY_ТУТ',`

### Крок 3: Запустіть
1. Подвійний клік на `index.html`

---

## 💡 Де побачити кожну функцію

### LiveSearch (пошук без кнопки)
```javascript
// script.js, рядки 64-79
searchInput.addEventListener('input', handleSearchInput);
searchTimeout = setTimeout(() => performSearch(query), 500);
```

### Інтеграція з API
```javascript
// script.js, рядки 84-115
const response = await fetch(url.toString());
const data = await response.json();
```

### Відображення результатів
```javascript
// script.js, рядки 137-155
function displayResults(movies) {
    resultsContainer.innerHTML = movies.map(movie => 
        createMovieCard(movie)
    ).join('');
}
```

### Адаптивний дизайн
```css
/* styles.css, рядки 514-582 */
@media (max-width: 768px) {
    .title { font-size: 2rem; }
    .results-grid { grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); }
}
```

### Обробка помилок
```javascript
// script.js, рядки 301-330
try {
    const data = await fetchMovies(query);
} catch (error) {
    handleError(error);
}
```
---

## 🎬 Приклади для тестування

Спробуйте ці пошуки:
```
"The Matrix"      → 150+ результатів
"Interstellar"    → Чудовий sci-fi
"Inception"       → Складна драма
"Avatar"          → Вражаючої графіки
"nonexistent"     → Обробка помилки
```