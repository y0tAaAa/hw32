# 🚀 Как отправить проект на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Введите название: `hw32`
3. Описание: "MovieFinder - Web app for movie search with LiveSearch and OMDb API"
4. Выберите: Public (чтобы я мог видеть)
5. НЕ инициализируйте README, .gitignore или license
6. Нажмите "Create repository"

## Шаг 2: Добавьте remote URL

Замените `YOUR_USERNAME` на ваше имя пользователя GitHub:

```bash
git remote add origin https://github.com/YOUR_USERNAME/hw32.git
git branch -M main
```

## Шаг 3: Отправьте на GitHub

```bash
git push -u origin main
```

## Альтернативно через HTTPS (если SSH не настроен):

```bash
# Если нужен токен вместо пароля:
git remote set-url origin https://YOUR_TOKEN@github.com/YOUR_USERNAME/hw32.git
git push -u origin main
```

## Проверка

После успешного push проверьте:
- https://github.com/YOUR_USERNAME/hw32

Все файлы должны быть видны на GitHub!

---

## ℹ️ Git статус локально

**Текущий статус:**
- ✅ Репозиторий инициализирован
- ✅ Все файлы добавлены (8 файлов)
- ✅ Первый commit создан
- ⏳ Remote еще не добавлен (добавьте URL на шаге 2)

**Просмотр истории:**
```bash
git log --oneline
```

**Просмотр статуса:**
```bash
git status
```
